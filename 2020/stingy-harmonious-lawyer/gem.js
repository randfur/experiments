import {random, deviate} from './utils.js';

export class Gem {
  static createGem() {
    const height = 1 + random(4);
    const radius = 1 + random(2);
    const spokes = Math.ceil(3 + random(7));
    
  }
}