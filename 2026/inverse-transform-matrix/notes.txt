# 2026-06-29
- Building getWorldRay() method in HexLinesContext to get a world ray from screen space co-ordinates.
- Maybe needs to invert a transform matrix.
- Internet suggests for typical transform matrices this can be done without full inversion.
  - https://graphics.stanford.edu/courses/cs248-98-fall/Final/q4.html
  - Covers translation and rotation but not scale.
  - Going to ignore skew.
- Scale should be easily read from the diagonal.
- Will start out in 2D first.
- Take a jumbled set of transform operations and attempt to undo it from just the final matrix.
- Implemented:
  - Mat3 class.
  - Vec2 class.
  - Transform sequence.
  - Canvas display of points.
  - Inverse attempt.
- Not working.
  - Leaving out scale for now, just rotate and translate.
  - Not coming to correct values.
  - Needs investigation and comparison with above website.
  - Something seems to be wrong with Mat3.setMultiply(), giving the identity matrix on one side doesn't leave the other unaffected.
    - Had a couple of the parts flipped around (m and n on the RHS).
- Working for rotate and translate.
- Still figuring out how to deal with scale.
  - The scale is being applied a second time by the inverse transform.
    - Scale 0.1 in the transform.
    - 200,100 -> 2,1
- Figured it out:
  - Take one row of the rotation and get its inverse square length.
  - This will undo the scaling being done by the WIP inverse transform matrix as well as the scaling already done to the transformed point.
- TODO: Do the same for 3D in the ga library.
- TODO: Investigate skew.
  - It's probably just each row done separately.
  - Whether its row or column would depend on which side the vector is multiplied on probably.
