# 2026-06-28
- Renamed hsv to thsv because it's not linear like hsv is, t stands for trig.
- Added graph of RGB values to compare thsv with hsv.
  - Exposed another bug in the implementation, the basis vectors of the RGB circle were not orthogonal to 1,1,1.
    - The x,y,z -> z,-x,y swizzle only given non-parallel vectors, it doesn't give orthogonal vectors.
  - Saved comparison as: non-orthogonal-rgb-circle.png
- Fixed up code to compute orthogonal basis vectors for the RGB circle.
  - Comparison with hsv is nearly identical.
  - Can see a slight curve in the RGB lines so still worth calling it something different.
  - Implementation much more complex now, maybe not worth the complexity vs the linear approach.

# 2026-06-27
- HSV -> RGB algorithm.
- Simple approach:
  - The H angle gets a point on a circle around the axis 1,1,1.
    - Perpendicular vectors are easy to find:
      - 1,1,-1
      - 1,-1,1
  - Scale the values such that the max value is 255 and the min value is 0.
- Decreasing V decreases the max value.
- Decreasing S increases the min value proportionally within the available space.
  - Not sure about this one, needs experimentation.
- Implemented algorithm, it works.
- AI reviewed it and pointed out a flaw; the RGB will change with trig curves while the usual conversion algorithm is piecewise linear, this may have similar peak values but the inbetweens will be skewed.
  - Added comparison graphic at the bottom putting the hue computation of trig vs linear side by side.
